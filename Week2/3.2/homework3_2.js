fetch('homework2_1.json')    .then(response => {        return response.json()    })    .then(employees => {        let myTable = ``;        let myTable2 = ``;        let newEmployees = [{}];        for (let i in employees) {            newEmployees[i] = {...employees[i]}        }        const addYearSalary = row => (row['yearSalary'] = `${row['salary'] * 12}`);        const addNextSalary = row => (row['nextSalary'] = [parseInt(row['salary']),                parseInt(row['salary']) + row['salary'] * 0.1,                parseInt(row['salary']) + row['salary'] * 0.21]        );        const addAdditionalFields = employees => {            for (let i in employees) {                addYearSalary(employees[i]);                addNextSalary(employees[i])            }        };        addAdditionalFields(employees);        const tableHeader = employees[0];        const tableHeader2 = newEmployees[0];        myTable += `<tr>`;        myTable2 += `<tr>`;        for (let i in tableHeader) {            myTable += `<th>${i}</th>`        }        for (let i in tableHeader2) {            myTable2 += `<th>${i}</th>`        }        myTable += `</tr>`;        myTable2 += `</tr>`;        for (let i in employees) {            myTable += `<tr>`;            for (j in tableHeader) {                if (j !== 'nextSalary') {                    myTable += `<td>${employees[i][j]}</td>`                }                if (j === 'nextSalary') {                    let nextSalaryOrderList = ``;                    nextSalaryOrderList += `<td><ol>`;                    for (k in employees[i][j]) {                        nextSalaryOrderList += `<li>${employees[i][j][k]}</li>`                    }                    nextSalaryOrderList += `</ol></td>`;                    myTable += nextSalaryOrderList                }            }            myTable += `</tr>`        }        document.getElementById('myTable').innerHTML = myTable;        for (let i in newEmployees) {            myTable2 += `<tr>`;            for (let j in tableHeader) {                if (newEmployees[i][j] !== undefined) {                    myTable2 += `<td>${newEmployees[i][j]}</td>`                }            }            myTable2 += `<tr>`        }        document.getElementById('myTable2').innerHTML = myTable2    })    .catch(error => {        console.error('Error:', error)    });