function calculate() {
    // if (event.keyCode === 13) {
    let inputRef = document.getElementById('paperInputs1');
    let convertInput = parseInt(inputRef.value);
    if (convertInput !==) {
        document.getElementById('result').innerHTML = `<p>Pleases input the number.</p>`
    }
    else {
        document.getElementById('result').innerHTML = `<p>${convertInput}</p>`
    }
    //}
}

console.log();